name: Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Verify action syntax
        id: npm-version
        uses: "./"
        with:
          cmd: "npm -v"
